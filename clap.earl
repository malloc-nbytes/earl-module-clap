module Clap

@pub class Arg [lx, hyphens] {
    @pub let lx = lx;
    @pub let hyphens = hyphens;
}

@pub fn parse(args: list): list {
    let res = [];

    foreach arg in args {
        if arg[0] != '-' {
            res.append(Arg(arg, 0));
        }
    }

    return res;
}

